# 角色包規格（草案）

公開引擎和私人角色素材必須保持分離。

## V0 原型

原型讀取一張三格橫向 PNG：

1. `idle`：待機
2. `roll`：滾動／工作
3. `clap`：被摸與吃東西後拍手

路徑：

```text
private_pets/active/reference-sheet.png
```

## 下一版

正式角色包將使用 `pet.json` 描述不同動畫資料夾、FPS、循環方式、碰撞區域、顯示比例與錨點，不會在公開引擎中寫死任何私人角色名稱。

