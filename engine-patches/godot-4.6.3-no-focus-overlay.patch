diff --git a/platform/windows/display_server_windows.cpp b/platform/windows/display_server_windows.cpp
index 22aba8a..97d3c77 100644
--- a/platform/windows/display_server_windows.cpp
+++ b/platform/windows/display_server_windows.cpp
@@ -2363,10 +2363,10 @@ void DisplayServerWindows::_get_window_style(bool p_main_window, bool p_initiali
 	r_style = 0;
 	r_style_ex = WS_EX_WINDOWEDGE;
 	if (p_main_window) {
-		// When embedded, we don't want the window to have WS_EX_APPWINDOW because it will
-		// show the embedded process in the taskbar and Alt-Tab.
+		// Desktop overlays need to accept focus for native popup dismissal while
+		// remaining absent from the taskbar and Alt-Tab.
 		if (!p_embed_child) {
-			r_style_ex |= WS_EX_APPWINDOW;
+			r_style_ex |= WS_EX_TOOLWINDOW;
 		}
 		if (p_initialized) {
 			r_style |= WS_VISIBLE;
